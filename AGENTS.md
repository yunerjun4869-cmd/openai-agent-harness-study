# OpenAI Agent 工程课程工作指南

- 文档、注释、报错和面向学习者的说明优先使用中文。
- 每次文件写入或补丁不得超过 200 行；大文件分批更新。
- 保留 s01–s17 递进结构，每章必须有 README.md 和可运行的 code.py。
- 新实现统一采用 OpenAI Responses API，完整保留 response.output 与 call_id。
- 不把模拟响应描述成真实模型结果；每章提供明确标识的 --demo 离线演示。
- 共用机制放入 harness 包，章节保留可读的接入代码和解释。
- API Key 只从环境变量或本目录 .env 读取，不写进示例、日志或测试。
- 不自动安装依赖，不调用 sudo；不在导入模块时调用网络或运行命令。
- 文件工具限定工作目录；执行任意程序不等于安全沙箱，必须明确边界。
- 测试关注协议、权限、并发、失败恢复与验证证据，不用假测试替代实际验证。
- 修改结束后运行离线测试和全章节演示；真实 API 验证必须如实报告是否执行。
